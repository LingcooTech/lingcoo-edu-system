<custom-navbar title="消息通知" show-back="{{true}}" />
<view class="page">
  <skeleton wx:if="{{loading}}" variant="list" />
  <view wx:elif="{{needLogin}}" class="empty login-empty">
    请先在“我的”登录
    <button class="btn btn-primary login-empty-btn" bindtap="goLogin">去登录</button>
  </view>
  <block wx:else>
    <view class="card panel">
      <view class="section-title">课程提醒</view>
      <view class="row-meta subscribe-hint">订阅后可在微信收到课程前提醒、服务次数扣减和学习动态通知。</view>
      <button
        class="btn btn-primary subscribe-btn"
        loading="{{subscribingReminders}}"
        disabled="{{subscribingReminders}}"
        bindtap="onSubscribeLessonNotifications"
      >
        {{subscribingReminders ? '请求中...' : '订阅提醒'}}
      </button>
    </view>

    <view class="section card panel">
      <view class="section-title">站内通知</view>
      <view wx:if="{{notifications.length}}" class="item-list">
        <view wx:for="{{notifications}}" wx:key="id" class="notification-row">
          <view class="row-between">
            <view class="row-title">{{item.title}}</view>
            <view class="notice-status {{item.status}}">{{item.statusLabel}}</view>
          </view>
          <view wx:if="{{item.body}}" class="notice-body">{{item.body}}</view>
          <view class="notice-footer">
            <view class="row-meta">{{item.createdAtLabel}}</view>
            <button
              wx:if="{{item.status == 'unread'}}"
              class="mini-button"
              data-id="{{item.id}}"
              bindtap="onMarkNotificationRead"
            >
              已读
            </button>
          </view>
        </view>
      </view>
      <view wx:else class="empty compact-empty">暂无通知</view>
    </view>
  </block>
</view>
